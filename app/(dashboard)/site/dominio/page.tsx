import { SitePlaceholder } from "../_components/SitePlaceholder";

export default function SiteDominioPage() {
  return <SitePlaceholder titulo="Domínio" descricao="Domínio próprio (mantendo suas URLs) ou subdomínio do UseFokio para o seu site." />;
}
