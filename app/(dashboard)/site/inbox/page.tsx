import { SitePlaceholder } from "../_components/SitePlaceholder";

export default function SiteInboxPage() {
  return <SitePlaceholder titulo="Inbox" descricao="Mensagens recebidas pelo formulário de contato do site (viram leads no CRM numa fase seguinte)." />;
}
