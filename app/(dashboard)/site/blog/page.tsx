import { SitePlaceholder } from "../_components/SitePlaceholder";

export default function SiteBlogPage() {
  return <SitePlaceholder titulo="Blog" descricao="Posts do blog do seu site. URL pública: /post/{id}-{slug}." />;
}
